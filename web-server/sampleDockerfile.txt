# Use a base image with a webserver and PHP pre-installed
FROM php:8.0-apache


# Install the MSSQL Server driver
RUN apt-get update && apt-get install -y \
    apt-transport-https \
    gnupg2 \
    libssl-dev \
    libcurl4-openssl-dev \
    unixodbc \
    unixodbc-dev \
    && pecl install sqlsrv-5.11.0 \
    && pecl install pdo_sqlsrv-5.11.0 \
    && docker-php-ext-enable sqlsrv pdo_sqlsrv \
    && apt-get clean && rm -rf /var/lib/apt/lists/*

RUN apt-get update && \
    #apt-get install -y gnupg2 && \
    curl https://packages.microsoft.com/keys/microsoft.asc | apt-key add - && \
    curl https://packages.microsoft.com/config/debian/11/prod.list > /etc/apt/sources.list.d/mssql-release.list && \
    apt-get update && \
    ACCEPT_EULA=Y apt-get install -y --no-install-recommends msodbcsql18 
   # mssql-tools && \
   # echo 'export PATH="$PATH:/opt/mssql-tools/bin"' >> ~/.bashrc && \
   # source ~/.bashrc \



# Copy the PHP script to the web-root folder
COPY ./index.php /var/www/html/


#RUN chown -R www-data:www-data /var/www/html \
    #&& chmod -R 755 /var/www/html




# Expose port 80 for HTTP traffic
EXPOSE 8000

# Enable Apache modules and restart the webserver
#RUN a2enmod rewrite headers && service apache2 restart